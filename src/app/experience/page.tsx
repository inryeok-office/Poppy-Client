// 인트로 CTA('체험하러 가기')의 도착지 자리표시.
// 실제 체험 화면(블록 코딩 에디터, Figma node 21:520)은 2페이지 작업에서 채운다.
export default function ExperiencePage() {
  return (
    <main className="flex min-h-full flex-1 flex-col items-center justify-center gap-2 bg-[#fbf4ea] p-8 text-center">
      <h1 className="text-2xl font-semibold text-[#39120b]">체험 화면 준비 중</h1>
      <p className="text-sm text-[#4d4742]">2페이지 작업에서 블록 코딩 에디터가 들어갑니다.</p>
    </main>
  );
}
